# FastAPI, SQLAlchemy, dotenv 등을 설치하기 위한 패키지 목록
fastapi
uvicorn
sqlalchemy
python-dotenv
## 비밀번호 해시화를 위한 인스톨
pip install passlib
# 이메일 필드 검증 인스톨
pip install pydantic[email]
# bcrypt 패키지 설치(비밀번호 해싱)
pip install bcrypt==3.2.0

pip install pydantic-settings

pip install itsdangerous

# jwt 인증 라이브러리
pip install pyjwt

# 소셜 로그인을 위한 인스톨
pip install requests

pip install python-multipart

pip install openai
